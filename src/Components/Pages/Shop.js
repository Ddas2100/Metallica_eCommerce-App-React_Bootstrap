import ProductList from "../Layout/ProductList";
import Section from "../UI/Section";

const Shop = () => {
    return (
        <Section>
            <ProductList />
        </Section>
    );
};

export default Shop;